const mul = require('../src/mul');

test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
test('test for mul func', () => {
  expect(mul(1, 1)).toBe(1);
});
